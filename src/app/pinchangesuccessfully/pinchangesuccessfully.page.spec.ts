import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PinchangesuccessfullyPage } from './pinchangesuccessfully.page';

describe('PinchangesuccessfullyPage', () => {
  let component: PinchangesuccessfullyPage;
  let fixture: ComponentFixture<PinchangesuccessfullyPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PinchangesuccessfullyPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
