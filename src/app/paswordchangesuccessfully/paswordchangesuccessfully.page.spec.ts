import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PaswordchangesuccessfullyPage } from './paswordchangesuccessfully.page';

describe('PaswordchangesuccessfullyPage', () => {
  let component: PaswordchangesuccessfullyPage;
  let fixture: ComponentFixture<PaswordchangesuccessfullyPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PaswordchangesuccessfullyPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
