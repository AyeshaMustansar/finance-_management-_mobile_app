import { ComponentFixture, TestBed } from '@angular/core/testing';
import { FingerprintchangesuccessfullyPage } from './fingerprintchangesuccessfully.page';

describe('FingerprintchangesuccessfullyPage', () => {
  let component: FingerprintchangesuccessfullyPage;
  let fixture: ComponentFixture<FingerprintchangesuccessfullyPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(FingerprintchangesuccessfullyPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
